 events{

}

http{
     upstream backend{
            server firstgateway:9000;
            server secondgateway:9000;
            server thirdgateway:9000;
            

                                
 }
    server{
        listen 80;

        location /{
            proxy_pass http://backend;
        }
    }
}